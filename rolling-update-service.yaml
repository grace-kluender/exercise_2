apiVersion: v1
kind: Service
metadata:
  name: nginx-application-service
spec:
  selector:
    app: nginx-application
    env: test
  ports:
  - name: http
    port: 80
    targetPort: 80
  type: NodePort